# LiusHub 代码审计报告

> 审计范围：`src/` 全部源码（主进程 6 个引擎 + preload + 渲染层 8 个视图/组件/store）、构建配置、README。
> 审计日期：2026-08-30 · 基于当前 `main` 工作区（v0.1.0 MVP）。
> 标注规则：🔴 需尽快修复的功能 Bug · 🟠 边界/质量问题 · 🔵 安全加固 · ⚡ 性能优化 · 🏗️ 架构与可维护性

## 修复进展（2026-08-30，两轮）

✅ **第一轮已修复**：P1-1 ~ P1-10 全部、P2-1/2/3/4/5/6/8、S-1~S-6、N-2/N-3/N-5（Home 并行）、A-5（errorHandler 兜底）、A-6（npmRebuild）、A-7 全部小项、文档不一致 1/2。

✅ **第二轮已修复（原"未实施"项）**：
- **P2-7** Docker SSH 直连：确认 docker-modem 内置 `docker system dial-stdio` 支持，修正 sshOptions（`agentForward: false`，条件化 privateKey/passphrase）；资产表单为 Docker 类型新增「SSH 直连」开关（`secret.useSsh`）
- **P2-9** Redis 断连广播：新增 `DB_STATUS` 通道，主进程连接级错误主动推送，数据库视图即时重置失效会话
- **P2-10 / N-4** SQLite 迁移至 worker 线程（`sqlite-worker.js`，每连接一个 worker）：大查询不再阻塞主进程；RPC 自带 30s 超时并通过 `db.interrupt()` 跨线程中断长查询；MySQL/PG/Redis/ClickHouse 查询统一 30s 竞速超时兜底
- **P2-3 附带** SFTP 字节级进度推送：`SFTP_PROGRESS` 通道按 1% 节流推送，SFTP 视图进度条显示真实进度
- **A-3** Docker stats 在 keep-alive 切走时通过 `onDeactivated` 停止推送（`docker.stopStats`）
- **A-2** 新增 `useConnectionSessions` composable，SFTP/Docker/数据库三个视图的连接生命周期（连接/原地重连/复制/关闭/配置缓存）统一收敛，消除约 300 行重复样板（SSH 视图因终端耦合保留独立实现）
- **A-1（第一阶段）** 新建 `ResultGrid.vue` 组件，主结果区与命令列控制台复用；`main.js` 图标改为按需注册（300+ → 44 个）
- **N-1（部分）** Element Plus 组件仍全量引入（`app.use(ElementPlus)`）；完整的 unplugin 按需方案因环境 npm 缓存权限受限无法安装依赖，待 `npm install -D unplugin-vue-components unplugin-auto-import` 后接线 vite.config.js 即可

⏳ **剩余待办**：
- A-1 后续：DatabaseView 其余对话框（表设计器/数据字典/ER 图/搜索）继续拆分
- N-1 完整版：按需引入 Element Plus 组件（需可用的 npm 环境）

---

## 总体评价

项目整体质量高于一般 MVP 水平：IPC 分层清晰、contextIsolation 正确开启、资产加密存储有降级兜底、写盘做了原子替换与串行化、PostgreSQL 跨库限制等平台差异都有注释说明。但仍存在 **10 个用户可直接感知的功能 Bug**（其中 4 个导致对应功能完全不可用）、若干安全隐患和明确的性能优化空间。

---

## 一、功能 Bug

### 🔴 P1-1 顶栏「折叠侧边栏」按钮完全失效
- **位置**：`src/renderer/App.vue:14`、`src/renderer/components/TopBar.vue:3`
- **现象**：点击顶栏第一个按钮无任何反应。
- **原因**：TopBar `emit('toggleSidebar')`，但 App.vue 中 `<TopBar :theme="theme" @toggle-theme="toggleTheme" />` 既没监听 `toggle-sidebar` 事件，也没把 `collapsed` 传进去（prop 为 undefined，图标永远显示 Fold）。
- **修复**：
  ```html
  <TopBar :theme="theme" :collapsed="sidebarCollapsed"
          @toggle-sidebar="sidebarCollapsed = !sidebarCollapsed"
          @toggle-theme="toggleTheme" />
  ```

### 🔴 P1-2 SSH「复制连接」后标题重复拼接
- **位置**：`src/renderer/views/SshView.vue:409-414`（copySession）+ `SshView.vue:283`（rename）
- **现象**：复制连接成功后 tab 标题变成 `root@host:22root@host:22 (副本)`。
- **原因**：copySession 把整个标题 `(s?.title) + ' (副本)'` 当作 `suffix` 传给 `doConnect`，而 doConnect 内部 rename 又拼了一次完整的 `user@host:port` 前缀。
- **修复**：`doConnect({ ...entry.cfg }, ' (副本)')`。

### 🔴 P1-3 PostgreSQL 新建 / 删除数据库必定失败
- **位置**：`src/renderer/views/DatabaseView.vue:1592-1593`（createDatabase）、`1609`（dropDatabase）
- **原因**：两处硬编码反引号包裹库名：`` CREATE DATABASE \`test\` ``，这是 MySQL 语法；PostgreSQL 会直接报语法错误（只支持双引号）。而项目其他地方都按 `dbType` 正确选了引号（见 `quoteId`）。
- **修复**：改用 `quoteId(value)` / 按 `s.dbType` 选择包裹符。

### 🔴 P1-4 「运行 SQL 文件」执行多语句脚本必失败
- **位置**：`src/renderer/views/DatabaseView.vue:1670-1686`（runSqlFile）+ `src/main/engines/db-engine.js:241-248`（mysql.connect）
- **原因**：整个 `.sql` 文件内容被当作**单条** SQL 提交。MySQL2 未开启 `multipleStatements`，pg 的 `query()` 也只接受单语句——含多个 `;` 的脚本会报 1064 语法错误。README 宣称支持"运行 SQL 文件"。
- **修复**：主进程新增"脚本执行"通道：按语句切分（需处理字符串/注释内的分号）逐条执行并汇总结果；MySQL 可选开启 `multipleStatements`（注意 SQL 注入面扩大）。

### 🔴 P1-5 Docker 日志带乱码前缀（未 demux）
- **位置**：`src/main/engines/docker-engine.js:84-89`
- **原因**：非 TTY 容器的 `logs()` 返回多路复用流，stdout/stderr 帧各有 8 字节头部，`buf.toString('utf-8')` 把头部一起输出，日志开头/中间出现不可见字符与乱码。
- **修复**：
  ```js
  const stream = await c.logs({ stdout: true, stderr: true, tail, follow: false })
  const chunks = []
  const demux = new PassThrough() // 或手动按 8 字节帧剥离
  c.modem.demuxStream(stream, stdoutAcc, stderrAcc)
  ```

### 🔴 P1-6 Docker 启动/停止容器失败时无任何提示
- **位置**：`src/renderer/views/DockerView.vue:354-363`
- **原因**：`startContainer/stopContainer` 中 `await window.liushub.docker.start(...)` 没有 try/catch，失败（如容器已在运行、权限不足）变成 unhandled rejection，用户看不到错误，还可能误以为操作成功。
- **修复**：包 try/catch，失败时 `ElMessage.error`。

### 🔴 P1-7 SQLite 建表勾选多列主键时失败
- **位置**：`src/renderer/views/DatabaseView.vue:1245`（colDdl SQLite 分支）+ `1253-1259`（buildCreateTable）
- **原因**：SQLite 分支给每个 `pk` 列内联 `PRIMARY KEY`；勾两列以上时生成 `a INTEGER PRIMARY KEY, b TEXT PRIMARY KEY`，SQLite 报 "table has more than one primary key"。表级 `PRIMARY KEY(...)` 兜底只对非 SQLite 生效。
- **修复**：SQLite 改为仅在表级生成 `PRIMARY KEY (a, b)`，列内联去掉。

### 🔴 P1-8 SQLite 打开不存在的文件会静默新建空库
- **位置**：`src/main/engines/db-engine.js:150-154`
- **现象**：路径输错时"连接成功"，但看到的是新建的空库，用户误以为数据丢失。
- **修复**：`connect` 前检查 `fs.existsSync(cfg.database)`（` :memory:` 除外），不存在则抛出明确错误。

### 🔴 P1-9 SFTP 连接失败时错误状态可能标到别的会话
- **位置**：`src/renderer/views/SftpView.vue:234`
- **原因**：`connectWith` 的 catch 里写的是 `setStatus(activeId.value, 'error')`；若连接期间用户切换了 tab，错误状态会标到不相干会话上。同函数上方明明已有局部变量 `s`。
- **修复**：`setStatus(s.id, 'error')`。

### 🔴 P1-10 资产加密回退/跨环境时 secret 静默失效
- **位置**：`src/main/secure-store.js:102-114`（decryptSecret）
- **场景**：vault.json 从"OS 加密可用"环境拷到"不可用"环境（如换机器、DPAPI 用户上下文变化），解密失败后：blob 是字符串但加密不可用时**原样返回 base64 字符串**；解密异常时返回 `null`。渲染层拿到的 `secret.password` 为 undefined，表现为"密码怎么填都不对"，无任何提示。
- **修复**：区分"未加密的明文对象"（直接返回）与"加密 blob 但解密失败"（抛出明确错误，提示用户重新录入密码），不要静默降级。

### 🟠 P2-1 SSH 终端中文/Emoji 跨 chunk 乱码
- **位置**：`src/main/engines/ssh-engine.js:164-166`
- **原因**：`chunk.toString('utf-8')` 在多字节字符恰好被 TCP 分块切割时产生乱码。
- **修复**：IPC 直接传 `Buffer`（IPC 原生支持），渲染层 `term.write(new Uint8Array(data))`，UTF-8 拼接交给 xterm 处理。

### 🟠 P2-2 Docker stats 按 chunk 直接 JSON.parse
- **位置**：`src/main/engines/docker-engine.js:105-109`
- **原因**：假设一个 `data` 事件 = 一条完整 JSON。流被分块时解析失败被 `catch {}` 静默吞掉，表现为资源面板偶发卡住不更新。
- **修复**：维护字符串缓冲，攒齐合法 JSON（括号配对或按流式解析器）后再 parse。

### 🟠 P2-3 SFTP 传输失败留半截文件、错误时流未销毁
- **位置**：`src/main/engines/sftp-engine.js:82-110`
- **问题**：upload/download 任一侧 error 后另一侧流未 `destroy()`；上传中断时远端留下不完整的文件且无清理；全程无字节级进度（UI 只有 0→100 跳变）。
- **修复**：错误时双向 destroy；可选：利用已有的 `written` 计数通过 IPC 推送真实进度；失败时删除远端残件（或提示）。

### 🟠 P2-4 SFTP 删除非空目录失败且无提示
- **位置**：`src/main/engines/sftp-engine.js:64-75`
- **问题**：只做 `rmdir`，非空目录直接报底层错误。建议前端确认文案说明"目录需为空"，或实现递归删除（危险操作需二次确认）。

### 🟠 P2-5 SQLite「设计表」重建方案丢失索引/触发器
- **位置**：`src/renderer/views/DatabaseView.vue:1274-1287`
- **问题**：SQLite 改表结构走"建临时表→拷数据→DROP 原表→RENAME"，只拷数据列；原表的索引、触发器、视图依赖全部静默丢失。
- **修复**：重建前导出 `sqlite_master` 中该表索引并重建，至少在保存前警示。

### 🟠 P2-6 CSV 导出：10 万行静默截断 + 公式注入 + 内存峰值
- **位置**：`src/main/engines/db-engine.js:124`（硬编码 `LIMIT 100000`，无提示）、`131-139`（转义未防公式注入）、`137-139`（全量 lines 数组 join）
- **修复**：截断时在返回值中告知实际行数并提示；单元格以 `= + - @` 开头时前缀 `'` 转义；大表改流式写（逐行 `write`）。

### 🟠 P2-7 Docker「SSH 方式连接」实际不可用
- **位置**：`src/main/engines/docker-engine.js:24-37`（注释已自认）+ `src/renderer/views/DockerView.vue:173-183`
- **问题**：dockerode 的 ssh 协议依赖本机 ssh-agent，带密码/私钥的资产配置连不上；UI 却允许创建此类资产并连接。
- **修复**：短期在 UI 明确标注"需本机 ssh-agent"；中期用 ssh2 自建 TCP 隧道后走 `socketPath`/本地端口。

### 🟠 P2-8 退出时只清理 SSH 连接
- **位置**：`src/main/index.js:212-216`
- **问题**：`window-all-closed` 只调 `sshEngine.disposeAll()`，DB/Docker/SFTP 连接靠进程退出兜底；一旦将来支持"关闭窗口驻留托盘"（darwin 分支已预留），这些连接全部泄漏。
- **修复**：补 `dbEngine`/`dockerEngine` 的 `disposeAll`，stats 流一并销毁。

### 🟠 P2-9 Redis 连接断开无感知
- **位置**：`src/main/engines/db-engine.js:494`
- **问题**：`client.on('error')` 只打日志，界面仍显示"已连接"，直到下次查询才报错。建议错误时向渲染层广播（可复用 DB list 的 `gone` 机制）。

### 🟠 P2-10 数据库查询无超时、无取消
- **位置**：`src/main/engines/db-engine.js`（query 全系列）
- **问题**：`SELECT * FROM 千万级大表` 会一直挂着 IPC，UI 无反馈也无法取消；SQLite（better-sqlite3 同步 API）还会**阻塞整个主进程**——期间所有窗口的所有 IPC（包括 SSH 终端输出转发）全部卡顿。
- **修复**：见 ⚡N-4（worker_threads）+ 为查询加可配置超时。

---

## 二、安全加固项

### 🔵 S-1 SSH 主机密钥不校验（MITM 风险）
- `src/main/engines/ssh-engine.js:97-103`：config 未设置 `hostVerifier`，任意中间人都能截获凭据。建议至少实现 TOFU（首连记录指纹、变更时告警），指纹可存入 vault.json。

### 🔵 S-2 IPC 文件系统无路径限制
- `src/main/engines/fs-engine.js:82-96`：`fs:read-text` / `fs:write-text` 允许读写**全盘任意路径**。渲染层一旦被注入（结合 S-4 的 XSS），等同于任意文件读写。建议：writeText 仅允许"本会话由保存对话框返回的路径"，或白名单目录。

### 🔵 S-3 数据库属性弹窗 XSS
- `src/renderer/views/DatabaseView.vue:1575-1580`：`dangerouslyUseHTMLString: true` 且库名/类型/数量未转义。库名来自远端数据库（攻击者可控）。改为纯文本渲染或 HTML 转义。

### 🔵 S-4 quickConnect 将明文密码写入 sessionStorage
- `src/renderer/views/AssetsView.vue:225`：完整 secret 放入 `sessionStorage('pending-ssh')`。XSS 场景可读取，且窗口存活期间一直保留。建议改为内存级传递（如 Pinia store 或主进程一次性 ticket）。

### 🔵 S-5 CSP 过宽
- `src/renderer/index.html:5`：`script-src 'unsafe-eval'`、`connect-src *`。dev 需要 eval，生产构建可按环境移除；`connect-src *` 建议收紧为空（渲染层无外联需求）。

### 🔵 S-6 主进程 IPC 参数未校验
- 各 `ipcMain.handle` 直接信任渲染层参数（未校验类型/长度/范围，如 `ssh:connect` 的 `port`、`fs:write-text` 的路径）。建议对关键入口做白名单校验，纵深防御。

---

## 三、性能优化

### ⚡ N-1 Element Plus 全量引入
- `src/renderer/main.js:3-5, 15-20`：全量注册组件 + 全部图标（几百个组件）。改用 `unplugin-vue-components` + `unplugin-auto-import` 按需引入，预计产物体积下降 50%+，首屏明显加快。

### ⚡ N-2 本地目录列表串行 stat
- `src/main/engines/fs-engine.js:22-32`：500 个条目逐个 `await stat`。改为 `Promise.all`（或 8~16 并发分批），大目录速度提升一个数量级。

### ⚡ N-3 查询结果全量过 IPC
- `src/renderer/views/DatabaseView.vue:244-247` 只渲染 500 行，但 db-engine 已把**全部结果**序列化传输（内存峰值 + IPC 阻塞）。建议：超过 N 行自动加 `LIMIT` 包装 / 服务端分页，至少超限截断时提示。

### ⚡ N-4 SQLite 放入 worker 线程
- better-sqlite3 是同步 API，跑在主进程会阻塞全部 IPC（含 SSH 终端流）。建议迁到 `worker_threads`，主进程经消息端口转发，顺带解决 P2-10 的"大查询卡全局"。

### ⚡ N-5 其他小项
- `src/renderer/views/Home.vue:61-74`：三次串行 await 改 `Promise.all`。
- `src/renderer/App.vue:28`：`<keep-alive>` 无上限，8 个视图可接受；若后续加重型视图建议 `:max`。
- `src/renderer/views/SshView.vue:227-231`：`fitWithRetry` 的 8 次 setTimeout 无清理，会话关闭后仍空跑（低危，顺手清理）。
- Docker stats 流在视图切走后仍持续推送，建议 `onDeactivated` 时销毁。

---

## 四、架构与可维护性

### 🏗️ A-1 DatabaseView.vue 单文件 2263 行
建议按区域拆分：连接树（DbTree.vue）、SQL 编辑器（QueryEditor.vue）、结果表（ResultGrid.vue）、各类对话框（表设计器/数据字典/ER 图/搜索）与各自 composables。当前任何改动都要在两千行里定位。

### 🏗️ A-2 四个视图重复的连接管理样板
SshView / SftpView / DockerView / DatabaseView 各自实现一套 `sessionMap + connectWith + reconnectSession + copySession + editSession + closeSession`（每份约 150 行，行为细节还不一致）。抽成 `useConnections()` composable 后，P1-9 这类不一致 bug 也会随之消失。

### 🏗️ A-3 keep-alive 下的清理逻辑是死代码
- `SshView.vue:184-196`、`SftpView.vue:205-210`、`DockerView.vue:165-171` 的 `onUnmounted` 在 keep-alive 下**永不执行**（直到窗口关闭），注释中"断开所有会话，避免连接累积泄漏"实际不生效。会话常驻是有意设计的话，应把断开入口放在显式按钮 + `window-all-closed` 统一清理；否则改用 `onDeactivated`。

### 🏗️ A-4 SSH 数据广播到所有窗口
- `ssh-engine.js:32-35` `sendToOwner` 实际是 `sendToAll`。当前单窗口无碍，多窗口时终端数据互相串扰且浪费 IPC 带宽。建议维护 sessionId→windowId 映射。

### 🏗️ A-5 工程化缺失
- 无 ESLint/Prettier 配置，无任何测试。至少为纯函数（`quoteId`/`colDdl`/`buildAlterTable`/CSV escape/`joinPath`）补单测——本报告多个 bug（P1-3、P1-7、P2-6）都属这类可被单测拦截的问题。
- `src/renderer/main.js` 未设置 `app.config.errorHandler`，未监听 `unhandledrejection`，错误兜底缺失。

### 🏗️ A-6 原生模块 ABI 脆弱（构建配置风险）
- 已实测当前环境 OK：better_sqlite3.node 按 Electron ABI 128 编译，`ELECTRON_RUN_AS_NODE` 下加载成功。
- 但 `package.json:47` `npmRebuild: false`——**新机器重新 `npm install` 后 prebuild 会拉 Node ABI 的二进制**，届时 dev 与打包产物中 SQLite 全线报 `NODE_MODULE_VERSION` 不匹配。建议改回 `npmRebuild: true`（或在 CI 中显式 `@electron/rebuild`），并在 README FAQ 增补此坑。

### 🏗️ A-7 小项
- 主题推送复用 `APP_GET_THEME` 通道（`main/index.js:49`），语义歧义，建议独立 `APP_THEME_CHANGED`。
- `TabBar.vue:11`：当前路由 tab 的图标用的是 `Loading`（转圈），语义误导。
- `StatusBar.vue:33-46`：CPU/MEM 是随机假数据，建议接真实数据或先移除，避免误导。
- `SessionTabs.vue` 右键菜单未做屏幕边缘钳制，靠近右/下边缘时菜单溢出。

---

## 五、文档与配置不一致

1. **README:37** 称 Windows 产物"NSIS + Portable"，实际 `package.json:58-60` 只有 `portable`（NSIS 未配置）。
2. **README:105** 称"CSP 收紧：禁止 inline 脚本"，实际 index.html 允许 `unsafe-inline` + `unsafe-eval`（见 S-5）。
3. **README** 项目结构未提及 `dist-renderer/`、`dist-msi/`、`build/msi-build.ps1`。

---

## 六、修复路线建议

| 批次 | 内容 | 预估 |
|------|------|------|
| 第 1 批（小改动高收益） | P1-1、P1-2、P1-3、P1-6、P1-8、P1-9、A-7 小项 | 半天 |
| 第 2 批（核心功能修复） | P1-4、P1-5、P1-7、P1-10、P2-1、P2-2、P2-3 | 1~2 天 |
| 第 3 批（安全加固） | S-1 ~ S-6 | 1~2 天 |
| 第 4 批（性能） | N-1、N-2、N-3、N-4 | 2~3 天 |
| 第 5 批（架构重构） | A-1、A-2、A-3 + 引入测试/ESLint | 按迭代排期 |

> P1-1 / P1-2 / P1-3 / P1-6 / P1-9 都是数行级别的修复，建议立即处理；P1-4 / P1-5 / P1-7 涉及 SQL 组装与流处理，修复时优先补上对应单元测试（A-5）。
